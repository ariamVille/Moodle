<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ibra.moodle">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Student" />
        <activity android:name=".Teacher" />
        <activity android:name=".StudentSubjectFolders" />
        <activity android:name=".StudentFinalGrades" />
        <activity android:name=".StudentSpecificSubject" />
        <activity android:name=".SubmitAssignment" />
        <activity android:name=".TeacherSubjectFolders" />
        <activity android:name=".TeacherSpecificSubject" />
        <activity android:name=".UploadMarks" />
        <activity android:name=".PostAnnouncement" />
        <activity android:name=".SendEmail"></activity>
    </application>

</manifest>
